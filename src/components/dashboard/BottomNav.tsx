import React from "react";
import { LayoutGrid, ClipboardList, Calendar, Settings } from "lucide-react";
import { Button } from "../ui/button";

const BottomNav = () => {
  return (
    <nav className="fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200"></nav>
  );
};

export default BottomNav;
